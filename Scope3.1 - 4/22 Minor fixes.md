Scope3.1 - 4/22> Minor fixes

1. I want the portal welcome email send from sales@toddssportinggoods.com
2. The box label is not printing properly.  
3. In quote builder > +Imprint(s) Button > imprints > to the right there is a blue button that opens the mockup builder. Remove that button.  
4. Quote Builder > +Imprint(s) i would love to be able to load images to the imprint box from there. (sometimes an actual mockup is not needed and just uploading an image)
5. Add the company logo at the top of all emails as a header and create a text block in the Communication Templates that will allow for email signatures.
6. In the customer profile make the state field in the address a drop down
7. Autofill street address
To make street names autofill in a website form, use HTML autocomplete attributes, browser-based address storage, or API services like Google Maps Autocomplete or Mapbox. The easiest method is adding autocomplete="street-address" to your <input> fields, prompting users to select saved data
8. The button in the garment purchase report there is a button that says mark as orders.  I need to be able to undo that if possible
9. Box label refinement


